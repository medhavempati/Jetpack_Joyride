def makeMando():
    height = 4
    width = 3
    form = [["M" for x in range(height)] for y in range(width)]

    return form

def makeBoss():
    height = 6
    width = 5
    form = [["B" for x in range(height)] for y in range(width)]

    return form